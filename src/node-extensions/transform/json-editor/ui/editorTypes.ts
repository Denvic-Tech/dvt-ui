export interface JsonEditorValues {
  auto_detect_record_path?: boolean;
  exclude_paths?: string[];
  explode_paths?: string[];
  json?: unknown;
  keep_json_paths?: string[];
  max_rows?: number;
  meta_paths?: string[];
  record_path?: string;
  separator?: string;
}
